# Daily-USD-CNY-Dashboard 项目分析文档

## 📌 项目概述

**项目名称**: USD 汇率监控看板  
**项目地址**: https://github.com/sean198604/Daily-USD-CNY-Dashboard  
**核心功能**: 从中国银行官网实时抓取美元（USD）对人民币（CNY）的现汇买入价，并提供可视化看板展示  
**技术栈**: Flask + ECharts + SQLite + APScheduler

---

## 🎯 核心功能模块

### 1. 汇率数据抓取 (fetch_usd_rate.py)

#### 数据来源
- **官方来源**: 中国银行官网 https://www.boc.cn/sourcedb/whpj/
- **抓取频率**: 定时任务（每天 9:31）+ 手动触发
- **数据类型**: USD 现汇买入价

#### 抓取逻辑
```python
fetch_usd_rate() -> {
    'date': 'YYYY-MM-DD',
    'pub_time': 'HH:MM:SS',
    'rate': float
}
```

**技术实现**:
- 使用 `requests` 库发送 HTTP GET 请求
- 使用 `BeautifulSoup` 解析 HTML 表格数据
- 正则表达式提取日期和时间格式
- 严格去重检查（基于 date + pub_time + rate 三元组）

**关键代码解析**:

```python
# 1. 请求中国银行汇率页面
resp = requests.get('https://www.boc.cn/sourcedb/whpj/', 
                    headers={'User-Agent': '...'}, 
                    timeout=15)

# 2. 解析表格，查找"美元"行
soup = BeautifulSoup(resp.text, 'html.parser')
for tr in soup.find_all('tr'):
    if cols[0] == '美元':
        rate = float(cols[1])  # 现汇买入价
        # 正则提取日期和时间
        d_match = re.search(r'(\d{4}[\./-]\d{2}[\./-]\d{2})', raw_full)
        t_match = re.search(r'(\d{2}:\d{2}:\d{2})', raw_full)

# 3. 去重检查
SELECT 1 FROM rates 
WHERE date = ? AND pub_time = ? AND rate = ? AND manual_flag = 0
```

### 2. 数据存储 (SQLite)

#### 数据库结构
```sql
CREATE TABLE rates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT,           -- 日期 YYYY-MM-DD
    pub_time TEXT,       -- 发布时间 HH:MM:SS
    rate REAL,           -- 汇率值
    manual_flag INTEGER DEFAULT 0  -- 0:自动抓取, 1:手动录入
)
```

#### 存储策略
- **自动抓取**: `manual_flag = 0`
- **手动录入**: `manual_flag = 1`
- **严格去重**: 避免重复记录同一条数据
- **时间戳**: 记录原始发布时间，用于后续筛选

### 3. 数据筛选算法 (app.py - get_best_rate_for_day)

**问题**: 中国银行可能一天内多次发布汇率数据，如何选择"当日标准汇率"?

**解决方案**: 三级优先级策略

```python
def get_best_rate_for_day(records, target_time):
    """根据优先级筛选一天的最佳汇率"""
    
    # 优先级1: 手动录入数据 (manual_flag = 1)
    manual_records = [rec for rec in records if rec['manual_flag'] == 1]
    if manual_records:
        return manual_records[0]
    
    # 优先级2: 无发布时间记录 (早期数据)
    no_pub_time = [rec for rec in records if not rec['pub_time']]
    if no_pub_time:
        return no_pub_time[0]
    
    # 优先级3: 发布时间最接近 09:31:00 的记录
    def time_diff(rec):
        t = datetime.strptime(rec['pub_time'], '%H:%M:%S').time()
        return abs(datetime.combine(datetime.min, t) - 
                   datetime.combine(datetime.min, target_time)).total_seconds()
    
    return min(records, key=time_diff)
```

**设计理念**:
- 09:31 接近中国银行工作时间，是最权威的发布时间
- 手动录入数据优先级最高，确保人工修正覆盖自动数据
- 兼容早期无发布时间的历史数据

### 4. Web 应用 (app.py)

#### Flask 路由结构

| 路由 | 方法 | 功能 | 输入 | 输出 |
|------|------|------|------|------|
| `/` | GET | 主页看板 | 无 | index.html |
| `/api/rates` | GET | 获取历史汇率 | days=60 | JSON数据 |
| `/admin` | GET | 管理后台 | 无 | admin.html |
| `/admin/add` | POST | 手动添加汇率 | date, rate | 重定向 |
| `/admin/delete/<id>` | POST | 删除记录 | id | 重定向 |
| `/api/fetch` | POST | 手动触发抓取 | 无 | 最新汇率JSON |

#### API 响应示例

**GET /api/rates?days=60**
```json
[
  {
    "date": "2026-03-01",
    "pub_time": "09:31:00",
    "rate": 7.2456
  },
  {
    "date": "2026-03-02",
    "pub_time": "09:30:15",
    "rate": 7.2512
  }
]
```

**POST /api/fetch**
```json
{
  "date": "2026-03-13",
  "pub_time": "15:30:20",
  "rate": 7.2485,
  "time": "15:30:20"
}
```

### 5. 定时任务 (APScheduler)

```python
scheduler = BackgroundScheduler()
scheduler.add_job(
    job_fetch_daily,           # 执行函数
    'cron',                    # 调度类型
    hour=9, minute=31,         # 每天 9:31
    misfire_grace_time=60      # 容错时间60秒
)
```

**配置说明**:
- **调度类型**: cron 表达式
- **执行时间**: 每天上午 9:31
- **容错机制**: `misfire_grace_time=60` 允许60秒内的延迟执行
- **后台运行**: 使用 BackgroundScheduler 不阻塞主线程

### 6. 前端可视化

#### 技术选型
- **图表库**: ECharts (百度开源)
- **样式**: 原生CSS (无框架)
- **布局**: CSS Grid + Flexbox

#### 页面布局结构

```
┌─────────────────────────────────────┐
│  [深色梯度块]       [Logo容器]       │  ← top-layout (1.2fr 0.8fr)
│  今日汇率: 7.2456                   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  [ECharts图表]                      │  ← chart-card
│  历史走势 (展示 90天)               │
│  滚轮缩放查看区间                   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  [实时同步信息]    [刷新按钮]      │  ← action-grid
│  抓取成功！实时汇率: 7.2485        │
└─────────────────────────────────────┘
```

#### ECharts 配置亮点

```javascript
chart.setOption({
  grid: { top: '8%', left: '3%', right: '3%', bottom: '5%', containLabel: true },
  tooltip: { trigger: 'axis' },
  xAxis: {
    type: 'category',
    axisLabel: { fontSize: 10, color: '#94a3b8' }
  },
  yAxis: {
    min: Math.floor(rawMin / 5) * 5,   // 自动计算Y轴范围
    max: Math.ceil(rawMax / 5) * 5,
    interval: 5,
    splitLine: { lineStyle: { color: '#f1f5f9' } }
  },
  series: [{
    data: rates,
    type: 'line',
    smooth: 0.3,                      // 曲线平滑度
    showSymbol: false,                // 不显示数据点
    lineStyle: { width: 3, color: '#2563eb' },
    areaStyle: {                      // 渐变填充
      color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
        { offset: 0, color: 'rgba(37,99,235,0.15)' },
        { offset: 1, color: 'rgba(37,99,235,0)' }
      ])
    }
  }]
});
```

#### 交互功能

1. **滚轮缩放**: 调整显示天数（7~180天）
   ```javascript
   document.addEventListener('wheel', (e) => {
     currentDays += (e.deltaY > 0 ? 5 : -5);
     currentDays = Math.max(7, Math.min(180, currentDays));
     loadHistoryData();
   });
   ```

2. **实时抓取**: 手动触发汇率更新
   ```javascript
   async function triggerRealtimeFetch() {
     const resp = await fetch('/api/fetch', { method: 'POST' });
     const res = await resp.json();
     // 显示最新汇率（保留4位小数）
     display.innerHTML = `实时汇率：<span>${res.rate.toFixed(4)}</span>`;
   }
   ```

3. **响应式设计**: 移动端适配
   ```css
   @media (max-width: 768px) {
     .top-layout, .action-grid { grid-template-columns: 1fr; }
     .logo-container { justify-content: center; margin-top: 10px; }
   }
   ```

---

## 🗂️ 项目文件结构

```
Daily-USD-CNY-Dashboard/
├── app.py                      # Flask 主程序 (163行)
├── fetch_usd_rate.py           # 汇率抓取模块 (115行)
├── requirements.txt            # Python依赖
├── Dockerfile                  # Docker镜像构建
├── docker-compose.yml          # Docker编排
├── rates.db                    # SQLite数据库
├── data.json                   # 备备份数据
├── screenshot.png              # 页面截图
├── README.md                   # 项目说明
├── LICENSE                     # MIT许可证
├── templates/
│   ├── index.html              # 主页看板 (279行)
│   └── admin.html              # 管理后台 (86行)
├── static/
│   └── LOGO.png                # 页面Logo
└── venv/                       # Python虚拟环境
```

---

## 📦 依赖分析

### 核心依赖包

| 包名 | 版本 | 用途 |
|------|------|------|
| **Flask** | - | Web框架，提供路由和模板渲染 |
| **Flask-APScheduler** | - | Flask集成的任务调度器 |
| **APScheduler** | - | 后台任务调度 |
| **requests** | - | HTTP请求库，用于抓取网页 |
| **beautifulsoup4** | - | HTML解析库 |
| **pandas** | - | 数据处理（可能用于导出） |
| **html5lib** | - | HTML5解析器 |
| **gunicorn** | - | WSGI服务器（生产环境） |
| **waitress** | - | Windows下的WSGI服务器 |

### 依赖说明

```txt
Flask                    # Web框架核心
Flask-APScheduler        # Flask定时任务扩展
APScheduler              # 任务调度引擎
requests                 # 网络请求
beautifulsoup4           # HTML解析
pandas                   # 数据分析
html5lib                 # HTML5支持
gunicorn                 # Linux生产服务器
waitress                 # Windows生产服务器
```

---

## 🚀 部署方式

### 1. 本地开发部署

#### macOS/Linux
```bash
cd Daily-USD-CNY-Dashboard
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
./venv/bin/python fetch_usd_rate.py  # 初始化数据库
./venv/bin/python app.py             # 启动服务
```

#### Windows
```bash
cd Daily-USD-CNY-Dashboard
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python fetch_usd_rate.py  # 初始化数据库
python app.py             # 启动服务
```

**访问地址**: http://localhost:5050  
**管理后台**: http://localhost:5050/admin

### 2. Docker部署

#### 构建镜像
```bash
docker build -t usd-cny-dashboard:latest .
```

#### 运行容器
```bash
docker run -d \
  --name usd-dashboard \
  -p 5050:5050 \
  -v $(pwd)/rates.db:/app/rates.db \
  --restart unless-stopped \
  -e TZ=Asia/Shanghai \
  usd-cny-dashboard:latest
```

#### Docker Compose (推荐)
```bash
docker-compose up -d
```

**优点**:
- 数据持久化（挂载 rates.db）
- 自动重启（restart: unless-stopped）
- 时区设置（TZ=Asia/Shanghai）
- 端口映射（5050:5050）

### 3. 生产环境部署

#### 使用 Waitress (Windows)
```python
# app.py 中已配置
from waitress import serve
serve(app, host='0.0.0.0', port=5050, threads=4)
```

#### 使用 Gunicorn (Linux)
```bash
gunicorn -w 4 -b 0.0.0.0:5050 app:app
```

---

## 🔧 核心技术要点

### 1. 数据抓取稳定性保障

**问题**: 网页结构可能变化，导致解析失败

**解决方案**:
```python
# 1. 多重容错机制
try:
    # 方案A: 正则提取
    d_match = re.search(r'(\d{4}[\./-]\d{2}[\./-]\d{2})', raw_full)
except:
    # 方案B: 强行截取
    clean_date = cols[6][:10].replace('.', '-').replace('/', '-')

# 2. 严格去重检查
SELECT 1 FROM rates 
WHERE date = ? AND pub_time = ? AND rate = ? AND manual_flag = 0

# 3. 超时设置
resp = requests.get(url, headers=headers, timeout=15)
```

### 2. 数据筛选算法设计

**核心思想**: 优先级排序 + 时间距离计算

```python
def get_best_rate_for_day(records, target_time):
    # 三级优先级:
    # 1. 手动录入 (manual_flag=1)
    # 2. 无发布时间 (历史数据)
    # 3. 最接近 target_time (09:31)
    
    # 时间差计算
    def time_diff(rec):
        t = datetime.strptime(rec['pub_time'], '%H:%M:%S').time()
        return abs(
            datetime.combine(datetime.min, t) - 
            datetime.combine(datetime.min, target_time)
        ).total_seconds()
    
    return min(records, key=time_diff)
```

### 3. 前端性能优化

**1. 去抖动处理**
```javascript
// 滚轮缩放加限流
document.addEventListener('wheel', (e) => {
  if (Math.abs(e.deltaY) < 10) return;  // 忽略小幅滚动
  // ... 更新逻辑
});
```

**2. 图表优化**
```javascript
// 不显示数据点，提升性能
showSymbol: false

// 曲线平滑度适中
smooth: 0.3

// 渐变填充视觉效果好
areaStyle: { color: new echarts.graphic.LinearGradient(...) }
```

**3. 自适应布局**
```javascript
// 监听窗口变化
window.addEventListener('resize', () => chart && chart.resize());
```

### 4. 数据库设计优化

**索引建议**:
```sql
-- 为常用查询创建索引
CREATE INDEX idx_date ON rates(date DESC);
CREATE INDEX idx_date_time ON rates(date, pub_time);
CREATE INDEX idx_manual ON rates(manual_flag);
```

**查询优化**:
```python
# 使用分页避免一次性加载所有数据
rows = conn.execute('''
    SELECT * FROM rates 
    ORDER BY date DESC, pub_time DESC 
    LIMIT 200
''').fetchall()
```

---

## 🎨 界面设计特色

### 1. 色彩系统

```css
:root {
  --primary-color: #2563eb;      /* 主蓝色 */
  --bg-color: #f1f5f9;            /* 浅灰背景 */
  --card-bg: #ffffff;            /* 卡片背景 */
  --text-main: #1e293b;          /* 主文本 */
  --text-sub: #64748b;           /* 副文本 */
}

/* 深色梯度块 */
background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
```

### 2. 布局策略

**顶部区域** (1.2fr 0.8fr):
- 左侧: 汇率显示块 (深色背景)
- 右侧: Logo容器 (透明背景)

**中间区域**:
- ECharts 图表 (白色卡片)

**底部区域** (1.2fr 0.8fr):
- 左侧: 实时同步信息
- 右侧: 刷新按钮

### 3. 视觉亮点

1. **巨大底纹**: USD文字水印
   ```css
   .hero-strip::after {
     content: 'USD';
     font-size: 5rem;
     color: rgba(255, 255, 255, 0.12);
   }
   ```

2. **渐变填充**: 图表区域渐变
   ```javascript
   color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
     { offset: 0, color: 'rgba(37,99,235,0.15)' },
     { offset: 1, color: 'rgba(37,99,235,0)' }
   ])
   ```

3. **微交互**: 按钮悬停效果
   ```css
   button:hover { 
     background: #1d4ed8; 
     transform: translateY(-1px); 
   }
   ```

---

## 📊 数据流转图

```
┌─────────────────┐
│ 中国银行官网     │
│ boc.cn/whpj/   │
└────────┬────────┘
         │ requests + BeautifulSoup
         ▼
┌─────────────────┐
│ fetch_usd_rate  │ ← 定时任务 (9:31)
│ 数据抓取模块     │   或手动触发
└────────┬────────┘
         │ 去重检查
         ▼
┌─────────────────┐
│ SQLite Database │
│ rates.db        │
└────────┬────────┘
         │ /api/rates
         ▼
┌─────────────────┐
│  Flask App      │
│  数据筛选算法   │
│  (三级优先级)   │
└────────┬────────┘
         │ JSON API
         ▼
┌─────────────────┐
│  ECharts 图表   │
│  前端可视化     │
└─────────────────┘
```

---

## 🔐 安全性考虑

### 1. 数据验证

```python
# 前端验证
<input type="date" name="date" required>
<input type="number" step="0.0001" name="rate" required>

# 后端验证
if date and rate:
    # 严格去重
    conn.execute('DELETE FROM rates WHERE date = ? AND manual_flag = 1', (date,))
```

### 2. SQL注入防护

```python
# 使用参数化查询
conn.execute('INSERT INTO rates(date, rate, manual_flag) VALUES (?, ?, 1)', 
            (date, rate))

# 禁止字符串拼接SQL
```

### 3. 请求限流

```python
# 建议添加 (当前未实现)
from flask_limiter import Limiter
limiter = Limiter(app, key_func=lambda: request.remote_addr)

@app.route('/api/fetch', methods=['POST'])
@limiter.limit("10/minute")
def api_fetch():
    # ...
```

---

## 🚨 潜在问题与改进建议

### 1. 当前问题

#### 问题1: 定时任务频率过低
**现状**: 每天只抓取一次 (9:31)  
**风险**: 如果中国银行在其他时间发布汇率，可能错过

**改进建议**:
```python
# 方案A: 增加抓取频率
scheduler.add_job(job_fetch_daily, 'interval', minutes=30)

# 方案B: 高频时间段密集抓取
scheduler.add_job(job_fetch_daily, 'cron', hour='9-16', minute='31')  # 每小时抓取
```

#### 问题2: 没有异常告警机制
**现状**: 抓取失败只在控制台打印日志  
**风险**: 运维人员无法及时发现问题

**改进建议**:
```python
# 添加邮件/钉钉/企业微信告警
import smtplib

def send_alert(msg):
    server = smtplib.SMTP('smtp.example.com', 587)
    server.sendmail('alert@example.com', 'admin@example.com', msg)
    server.quit()

def job_fetch_daily():
    try:
        fetch_usd_rate.fetch_and_store()
    except Exception as e:
        send_alert(f"汇率抓取失败: {e}")
```

#### 问题3: 数据库没有备份
**现状**: rates.db 在本地，无备份机制  
**风险**: 数据丢失无法恢复

**改进建议**:
```python
# 定期备份数据库
import shutil
from datetime import datetime

def backup_database():
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    shutil.copy2('rates.db', f'backup/rates_{timestamp}.db')
```

#### 问题4: 缺少数据导出功能
**现状**: 只能通过管理页面查看数据  
**风险**: 数据分析不便

**改进建议**:
```python
@app.route('/api/export')
def export_data():
    import pandas as pd
    conn = get_db_connection()
    df = pd.read_sql('SELECT * FROM rates ORDER BY date DESC', conn)
    conn.close()
    return df.to_csv(index=False)
```

### 2. 性能优化建议

#### 优化1: 添加缓存
```python
from flask_caching import Cache

cache = Cache(app, config={'CACHE_TYPE': 'simple'})

@app.route('/api/rates')
@cache.cached(timeout=300)  # 缓存5分钟
def api_rates():
    # ...
```

#### 优化2: 数据库连接池
```python
from sqlalchemy import create_engine
from sqlalchemy.pool import QueuePool

engine = create_engine('sqlite:///rates.db', 
                       poolclass=QueuePool, 
                       pool_size=5)
```

### 3. 功能扩展建议

#### 扩展1: 支持多币种
```python
# 当前只支持USD，可扩展到EUR、JPY等
CURRENCIES = ['USD', 'EUR', 'JPY', 'GBP']

def fetch_all_rates():
    for currency in CURRENCIES:
        # 抓取各个币种的汇率
        pass
```

#### 扩展2: 添加汇率预警
```python
# 设置上下限阈值
THRESHOLD_HIGH = 7.3000
THRESHOLD_LOW = 7.2000

def check_threshold(rate):
    if rate > THRESHOLD_HIGH:
        send_alert(f"汇率过高: {rate}")
    elif rate < THRESHOLD_LOW:
        send_alert(f"汇率过低: {rate}")
```

#### 扩展3: 添加历史数据对比
```javascript
// 前端显示同比、环比
const yesterdayRate = data[data.length - 2].rate;
const change = ((latestRate - yesterdayRate) / yesterdayRate * 100).toFixed(2);
display.innerHTML = `较昨日: ${change > 0 ? '+' : ''}${change}%`;
```

---

## 📝 代码质量评估

### 优点

1. ✅ **代码结构清晰**: 模块化设计，职责分明
2. ✅ **注释充分**: 关键逻辑都有注释说明
3. ✅ **错误处理**: 使用 try-except 包裹关键操作
4. ✅ **去重逻辑**: 严格的三元组去重，避免重复数据
5. ✅ **用户体验**: 滚轮缩放、实时抓取等交互流畅
6. ✅ **响应式设计**: 移动端适配良好
7. ✅ **Docker支持**: 提供完整的容器化部署方案

### 需要改进

1. ⚠️ **缺少单元测试**: 没有测试代码
2. ⚠️ **日志系统不完善**: 只使用 print 输出
3. ⚠️ **配置管理**: 硬编码的配置项（如数据库路径、端口）
4. ⚠️ **监控告警**: 缺少监控和告警机制
5. ⚠️ **文档**: 缺少API文档和开发文档

---

## 🔍 关键代码片段解析

### 1. 汇率抓取核心逻辑

```python
# fetch_usd_rate.py 第7-69行
def fetch_usd_rate():
    url = 'https://www.boc.cn/sourcedb/whpj/'
    headers = {
        'User-Agent': 'Mozilla/5.0 ...'
    }
    
    # 1. 发送HTTP请求
    resp = requests.get(url, headers=headers, timeout=15)
    resp.encoding = 'utf-8'
    
    # 2. 解析HTML
    soup = BeautifulSoup(resp.text, 'html.parser')
    all_trs = soup.find_all('tr')
    
    # 3. 查找美元数据
    for tr in all_trs:
        tds = tr.find_all('td')
        if len(tds) < 8: continue
        
        cols = [td.get_text(strip=True) for td in tds]
        
        if cols[0] == '美元':  # 找到美元行
            # 提取汇率
            rate = float(cols[1])
            
            # 正则提取日期和时间
            raw_full = f"{cols[6]} {cols[7]}".strip()
            d_match = re.search(r'(\d{4}[\./-]\d{2}[\./-]\d{2})', raw_full)
            t_match = re.search(r'(\d{2}:\d{2}:\d{2})', raw_full)
            
            return {
                'date': d_match.group(1).replace('.', '-').replace('/', '-'),
                'pub_time': t_match.group(1),
                'rate': rate
            }
    
    return None
```

**设计亮点**:
- 正则表达式匹配多种日期格式 (2026.01.16, 2026-01-16, 2026/01/16)
- 时间和日期分开提取，提高准确性
- 错误处理完善，返回 None 让调用者处理

### 2. 数据筛选算法

```python
# app.py 第22-50行
def get_best_rate_for_day(records, target_time):
    """
    三级优先级筛选:
    1. 手动录入 (manual_flag = 1)
    2. 无发布时间 (早期数据)
    3. 最接近 target_time (09:31)
    """
    
    # 优先级1: 查找手动记录
    manual_records = [rec for rec in records if rec['manual_flag'] == 1]
    if manual_records:
        return manual_records[0]
    
    # 优先级2: 查找无发布时间记录
    no_pub_time = [rec for rec in records if not rec['pub_time'] or 
                    str(rec['pub_time']).strip() == '']
    if no_pub_time:
        return no_pub_time[0]
    
    # 优先级3: 计算时间差，找最接近的
    def time_diff(rec):
        try:
            t = datetime.strptime(rec['pub_time'], '%H:%M:%S').time()
            return abs(
                datetime.combine(datetime.min, t) - 
                datetime.combine(datetime.min, target_time)
            ).total_seconds()
        except (ValueError, TypeError):
            return float('inf')
    
    return min(records, key=time_diff)
```

**设计亮点**:
- 优先级清晰，逻辑严谨
- 使用 `time_diff` 函数计算时间距离，精确到秒
- 异常处理：解析失败返回 `float('inf')`，确保不中断程序

### 3. Flask路由与数据流

```python
# app.py 第58-91行
@app.route('/api/rates')
def api_rates():
    days = request.args.get('days', default=60, type=int)
    target_time = time(9, 31)
    
    # 1. 查询数据
    conn = get_db_connection()
    rows = conn.execute('''
        SELECT id, date, pub_time, rate, manual_flag 
        FROM rates 
        ORDER BY date DESC, pub_time DESC
    ''').fetchall()
    conn.close()
    
    # 2. 按日期分组
    grouped = {}
    for row in rows:
        d = row['date']
        if d not in grouped:
            grouped[d] = []
        grouped[d].append(row)
    
    # 3. 筛选每天的最佳汇率
    result = []
    for d in sorted(grouped.keys(), reverse=True):
        chosen = get_best_rate_for_day(grouped[d], target_time)
        result.append({
            'date': chosen['date'],
            'pub_time': chosen['pub_time'] if chosen['pub_time'] else '',
            'rate': chosen['rate']
        })
    
    # 4. 截取天数并重新排序
    result = result[:days]
    result.reverse()  # 按日期正序
    
    return jsonify(result)
```

**设计亮点**:
- 按日期分组处理，一天多条记录合并为一条
- 先倒序取数据，再正序返回，逻辑清晰
- 支持 `days` 参数灵活控制返回数量

---

## 🎓 学习要点总结

### 1. Web爬虫技术

- 使用 `requests` 发送HTTP请求
- 使用 `BeautifulSoup` 解析HTML
- 正则表达式提取结构化数据
- User-Agent 设置避免被屏蔽
- 超时设置防止程序挂起

### 2. Flask框架应用

- 路由装饰器 (`@app.route`)
- 模板渲染 (`render_template`)
- JSON响应 (`jsonify`)
- 表单处理 (`request.form`)
- 重定向 (`redirect`, `url_for`)

### 3. 数据库操作

- SQLite轻量级数据库使用
- 参数化查询防止SQL注入
- 数据去重逻辑设计
- Row Factory使用（字典式访问）

### 4. 定时任务调度

- APScheduler基本使用
- cron表达式配置
- BackgroundScheduler后台运行
- 任务容错机制 (misfire_grace_time)

### 5. 前端可视化

- ECharts图表库使用
- CSS Grid布局
- Flexbox对齐
- 响应式设计
- JavaScript异步请求

### 6. Docker容器化

- Dockerfile编写
- 多阶段构建优化
- 数据卷挂载持久化
- Docker Compose编排

---

## 📚 参考资料

### 项目资源

- **GitHub仓库**: https://github.com/sean198604/Daily-USD-CNY-Dashboard
- **在线演示**: https://stardust1986.wuaze.com
- **数据来源**: https://www.boc.cn/sourcedb/whpj/

### 技术文档

- **Flask官方文档**: https://flask.palletsprojects.com/
- **APScheduler文档**: https://apscheduler.readthedocs.io/
- **ECharts文档**: https://echarts.apache.org/zh/index.html
- **BeautifulSoup文档**: https://www.crummy.com/software/BeautifulSoup/bs4/doc/

---

## 📌 总结

**Daily-USD-CNY-Dashboard** 是一个设计精良、功能完整的汇率监控看板项目。它展示了以下技术能力:

1. **Web爬虫**: 从中国银行官网实时抓取汇率数据
2. **数据存储**: SQLite持久化存储
3. **定时任务**: APScheduler自动调度
4. **Web应用**: Flask提供RESTful API
5. **数据可视化**: ECharts图表展示
6. **容器化部署**: Docker完整支持

**核心优势**:
- 代码结构清晰，易于维护
- 数据筛选算法严谨，优先级设计合理
- 前端交互流畅，用户体验良好
- Docker支持，部署方便

**适用场景**:
- 外贸企业汇率监控
- 投资者汇率趋势分析
- 财务部门汇率数据统计
- 个人理财汇率追踪

这是一个值得学习和参考的优秀项目！
