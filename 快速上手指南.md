# Daily-USD-CNY-Dashboard 快速上手指南

## 🚀 5分钟快速启动

### 方式一：本地运行（推荐开发者）

#### 1. 安装依赖
```bash
# 进入项目目录
cd C:/Users/Administrator/Documents/Github/Daily-USD-CNY-Dashboard

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
venv\Scripts\activate

# 安装依赖包
pip install -r requirements.txt
```

#### 2. 初始化数据库
```bash
python fetch_usd_rate.py
```
*首次运行会自动创建 rates.db 数据库，并抓取最新汇率*

#### 3. 启动服务
```bash
python app.py
```

#### 4. 访问应用
- **主页看板**: http://localhost:5050
- **管理后台**: http://localhost:5050/admin

---

### 方式二：Docker运行（推荐生产环境）

#### 1. 构建镜像
```bash
docker build -t usd-cny-dashboard:latest .
```

#### 2. 运行容器
```bash
docker run -d \
  --name usd-dashboard \
  -p 5050:5050 \
  -v $(pwd)/rates.db:/app/rates.db \
  --restart unless-stopped \
  -e TZ=Asia/Shanghai \
  usd-cny-dashboard:latest
```

#### 3. 使用 Docker Compose（最简单）
```bash
docker-compose up -d
```

---

## 📖 核心功能说明

### 1. 自动抓取
- **定时任务**: 每天上午 9:31 自动抓取中国银行最新汇率
- **手动触发**: 点击页面底部的"执行实时同步"按钮立即抓取

### 2. 数据展示
- **今日汇率**: 显示最接近 9:30 的汇率作为当日标准汇率
- **历史走势**: ECharts 图表展示历史汇率变化
- **时间范围**: 支持滚动鼠标滚轮调整显示天数（7~180天）

### 3. 数据管理
- **手动录入**: 管理后台可以手动添加历史汇率
- **删除记录**: 可以删除错误或重复的记录
- **优先级**: 手动录入的汇率优先于自动抓取

---

## ⚙️ 配置说明

### 修改端口
编辑 `app.py` 第163行：
```python
serve(app, host='0.0.0.0', port=5050, threads=4)
# 将 5050 改为你想要的端口，例如 8080
```

### 修改定时任务时间
编辑 `app.py` 第149行：
```python
scheduler.add_job(job_fetch_daily, 'cron', hour=9, minute=31)
# hour=9, minute=31 表示每天 9:31
# 改为 hour=10, minute=0 表示每天 10:00
```

### 修改数据库路径
编辑 `app.py` 第13行：
```python
app.config['DB_PATH'] = 'rates.db'
# 可以改为绝对路径，例如：'C:/data/rates.db'
```

---

## 🔧 常见问题

### 1. 抓取失败怎么办？

**症状**: 点击"执行实时同步"提示错误或无反应

**排查步骤**:
1. 检查网络连接
2. 访问 https://www.boc.cn/sourcedb/whpj/ 确认网站可访问
3. 查看控制台错误日志

**解决方案**:
- 中国银行网站可能更新了页面结构
- 需要修改 `fetch_usd_rate.py` 中的解析逻辑
- 联系开发者获取更新

### 2. 定时任务没有执行？

**症状**: 第二天早上发现汇率没有更新

**排查步骤**:
1. 确认程序是否在运行
2. 检查系统时间是否正确
3. 查看控制台是否有定时任务启动日志

**解决方案**:
- 确保程序 24/7 运行（推荐使用 Docker）
- 使用 `pm2` 或 `supervisor` 管理进程
- 检查定时任务配置是否正确

### 3. 数据库文件丢失？

**症状**: 页面显示无数据

**解决方案**:
1. 重新运行 `python fetch_usd_rate.py` 初始化数据库
2. 如果有备份，恢复备份文件
3. 建议定期备份数据库文件

### 4. Docker 容器无法访问？

**症状**: http://localhost:5050 打不开

**排查步骤**:
1. 确认容器是否运行: `docker ps`
2. 查看容器日志: `docker logs usd-dashboard`
3. 检查端口是否被占用: `netstat -ano | findstr 5050`

**解决方案**:
- 重启容器: `docker restart usd-dashboard`
- 修改端口映射: `-p 8080:5050`
- 检查防火墙设置

---

## 📊 数据导出

### 导出为 CSV
可以通过 SQLite 命令行导出：
```bash
sqlite3 rates.db
> .headers on
> .mode csv
> .output export.csv
> SELECT * FROM rates;
> .quit
```

### 导出为 Excel
使用 Python 脚本：
```python
import pandas as pd
import sqlite3

conn = sqlite3.connect('rates.db')
df = pd.read_sql('SELECT * FROM rates ORDER BY date DESC', conn)
df.to_excel('rates_export.xlsx', index=False)
conn.close()
```

---

## 🎨 自定义 Logo

### 替换 Logo 文件
1. 准备 Logo 图片（建议尺寸：200x80 像素）
2. 文件名保存为 `LOGO.png`
3. 放到 `static` 目录下
4. 重启服务

### 修改 Logo 大小
编辑 `templates/index.html` 第85行：
```css
.logo { 
  height: 70px;  /* 修改为你想要的高度 */
  width: auto; 
}
```

---

## 🔐 安全建议

### 1. 限制访问
修改 `app.py` 添加认证：
```python
from functools import wraps

def auth_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        auth = request.authorization
        if not auth or auth.username != 'admin' or auth.password != 'your_password':
            return Response('访问被拒绝', 401, {'WWW-Authenticate': 'Basic realm="Login Required"'})
        return f(*args, **kwargs)
    return decorated

@app.route('/admin')
@auth_required
def admin():
    # ...
```

### 2. 备份数据库
定期备份数据库文件：
```bash
# Windows 计划任务
schtasks /create /tn "BackupRatesDB" /tr "copy C:\path\to\rates.db C:\backup\rates_%date:~0,4%%date:~5,2%%date:~8,2%.db" /sc daily /st 02:00
```

### 3. 限制请求频率
添加限流中间件：
```bash
pip install Flask-Limiter
```

```python
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(app, key_func=get_remote_address)

@app.route('/api/fetch', methods=['POST'])
@limiter.limit("10/minute")
def api_fetch():
    # ...
```

---

## 📈 性能优化

### 1. 添加缓存
```bash
pip install Flask-Caching
```

```python
from flask_caching import Cache

cache = Cache(app, config={'CACHE_TYPE': 'simple'})

@app.route('/api/rates')
@cache.cached(timeout=300)  # 缓存5分钟
def api_rates():
    # ...
```

### 2. 数据库索引
在 SQLite 中添加索引：
```sql
CREATE INDEX idx_date ON rates(date);
CREATE INDEX idx_date_time ON rates(date, pub_time);
```

### 3. 减少数据加载量
限制查询结果数量：
```python
@app.route('/api/rates')
def api_rates():
    days = min(request.args.get('days', default=60, type=int), 90)  # 最多90天
    # ...
```

---

## 🚀 部署到生产环境

### 使用 Gunicorn (Linux)
```bash
pip install gunicorn

# 启动服务
gunicorn -w 4 -b 0.0.0.0:5050 app:app

# 后台运行
nohup gunicorn -w 4 -b 0.0.0.0:5050 app:app > app.log 2>&1 &
```

### 使用 Nginx 反向代理
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5050;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 使用 Supervisor 管理进程
```ini
[program:usd-dashboard]
command=/path/to/venv/bin/gunicorn -w 4 -b 0.0.0.0:5050 app:app
directory=/path/to/Daily-USD-CNY-Dashboard
user=www-data
autostart=true
autorestart=true
stdout_logfile=/var/log/usd-dashboard.log
```

---

## 📞 获取帮助

- **GitHub Issues**: https://github.com/sean198604/Daily-USD-CNY-Dashboard/issues
- **项目文档**: 查看同目录下的 `项目分析文档.md`
- **在线演示**: https://stardust1986.wuaze.com

---

## 📝 版本信息

- **项目版本**: 1.0
- **Python 版本**: 3.11+
- **最后更新**: 2026-03-13
- **作者**: sean198604

---

**祝使用愉快！** 🎉
