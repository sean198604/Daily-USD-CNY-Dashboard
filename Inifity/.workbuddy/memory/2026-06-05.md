# 2026-06-05 工作记录

## 诊断：trustlayer.free.nf 数据消失问题

**问题现象**：https://trustlayer.free.nf/ 打开显示没有数据

**排查结论**：
- FTP 上 index.html 中文字符乱码（上传时编码问题），但逻辑正常，不影响运行
- FTP 上 data.json 停在 Jun 4 21:00（昨天），今天计划任务虽然跑了但未更新到最新
- 根本原因：`upload_via_ftp_final.py` 的工作目录是项目根目录（`Daily-USD-CNY-Dashboard/`），生成的 `data.json` 在根目录，与 `Inifity/data.json` 是两个不同的文件
- `rates.db` 有完整数据到 2026-06-05，数据库本身正常

**修复操作**：
- 手动运行 `upload_via_ftp_final.py`，成功上传 314 条记录（到 2026-06-05）到 `/trustlayer.free.nf/htdocs/data.json`

## 重要说明
- 计划任务每天10:00自动跑 `upload_via_ftp_final.py`（在项目根目录执行），生成并上传 data.json 到 `/trustlayer.free.nf/htdocs/`
- `Inifity/data.json` 是老的备份文件，不是实际使用的那个
